---
title: "Prediction competition"
author: "Nelly Blindenbacher"
date: "June 2023"
output: pdf_document

---

**Part 1: Clinical Data**

```{r, echo=FALSE}
# Load packages into R
library(knitr)
library(xtable)
library(readxl)
library(rmarkdown)
library(tinytex)

# Patient selection
readxl::read_excel("") #Enter xlsx-file with Patient selection

# Inverse row and columns
df <- as.data.frame(clinicaldata_selection)
df_transpose = (t(df))

# Build the final table
knitr::kable(df_transpose[,1], #fill in patient followid individually
      format = "pipe",
      row.names = "TRUE", 
      col.names = "Study patient", 
      escape = FALSE)
```
